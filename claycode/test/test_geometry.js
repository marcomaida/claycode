import { area, pickPointOnPerimeter, segmentPolygonIntersections, 
         isPointInPolygon, circularity } from "../geometry/geometry.js"
import { assert_eq } from "./test_utils.js"

console.log("* Geometry");

try {
    // let square = 
    assert_eq(1,1)
}
catch(error) {
    console.error(`TEST FAILED: ${error}`)
} 