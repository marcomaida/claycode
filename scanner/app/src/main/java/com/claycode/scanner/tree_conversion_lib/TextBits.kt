package com.claycode.scanner.tree_conversion_lib

/**
 * This class provides conversions between text and bits.
 * Bits are represented as strings of zeros and ones.
 */
class TextBits {
    companion object {
        fun TextToBits(text: String): String {
            throw NotImplementedError()
        }

        fun BitsToText(bits: String): String {
            throw NotImplementedError()
        }
    }
}